<h2>Comments for the feed</h2>
<div>
  <% @comments.each do |comment| %>
    <div>
      <p><%= comment.created_by %> <%= comment.displayed_time %></p>
      <p><%= raw comment.text %></p>
    </div>
  <% end %>
</div>