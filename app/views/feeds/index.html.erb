<div id='scroll-content' class='container' style='background-color: #f6f6ef'>
  <h1>Hacker News</h1>

  <% @feeds.each do |feed| %>
    <div>
      <%= render partial: '/shared/feed', locals: {feed: feed} %>
    </div>
  <% end %>
</div>